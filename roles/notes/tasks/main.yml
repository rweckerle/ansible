---
- name: copy notes files
  unarchive:
    src: Notes_basic.tar.gz
    dest: /usr/share

- name: set permissions
  file:
    path: /usr/share/Notes_basic
    mode: '0777'
    owner: root
    recurse: true
